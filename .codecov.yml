---
# yaml-language-server: $schema=https://www.schemastore.org/codecov.json
coverage:
  status:
    project:
      default:
        target: 60%
        threshold: 40%
ignore:
  - "main.go"
  - "docs"
  - "scripts"
